/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          "www.milesweb.in",
          "img-c.udemycdn.com"
        ],
      },
};

export default nextConfig;
