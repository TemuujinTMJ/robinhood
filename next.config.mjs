/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          "www.milesweb.in",
          "img-c.udemycdn.com",
          "t4.ftcdn.net"
        ],
      },
};

export default nextConfig;
